{
  "name": "expense-budget-visualizer",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "vitest --run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "fast-check": "^3.22.0",
    "vitest": "^2.0.0"
  },
  "vitest": {
    "environment": "node"
  }
}
